# zip -r myarchive.zip dir1 -x dir1/ignoreDir1/**\* dir1/ignoreDir2/**\*
ls -la
# zip -r myarchive.zip * -x commit.sh fs.js app.js test\ zip/
# git archive -o $@.zip HEAD
# alias gitzip="git archive -o $@.zip HEAD"

